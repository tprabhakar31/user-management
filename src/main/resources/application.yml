spring:
  application:
    name: user-management

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mymart
    username: root
    password: Root@123

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8080

